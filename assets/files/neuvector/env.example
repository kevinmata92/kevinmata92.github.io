# CVE-2026-78428 PoC sample environment file.
# Copy to .env and customize.

# UI host (manager) - for manager race test
NV_UI="https://neuvector.domain.tld"

# ACS / Recipient / Audience - must match the SAML assertion exactly (UI host, not API host)
SAML_REDIRECT="https://neuvector.domain.tld/token_auth_server"